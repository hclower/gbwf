To Do List

[X] Write up rough roadmap

[X] Remove temp HS handler testing code
[X] Add utitities w/ 'wait for any button' function
[X] Add automated size display to makefile

- Error handling:
 [X] Restructure main function so that each state returns a new state
 [X] Test error state
 [X] Split state code into error manager & error state (for displaying err)
 [X] Modify error codes to work like errno (global variables, not passed)

[X] Figure out subpixel movement
[X] Test subpixel speed vs normal speed
   - Looks like using structures to do subpixel operations is actually slightly
     faster than doing it with bit-shifts
     - Makes sense, bit-shifts are fast but it's still an operation the structs
       are totally skipping. Both flavors are doing 16-bit math.
   - Bit shifting does allow a larger play area- 8.8 is good enough to track
     everything onscreen but will run into problems tracking objects offscreen.
     The Simple Physics demo means that the 'pixel' portion of each word can be
     in a 1024 x 1024 area.
   - This probably does not matter for GBWF which is effectively a single-screen
     game, but it's worth rememebring for the future.
[X] Find an extension to Sublime Text to generate Doxygen headers
[X] Game state skeleton

    Some notes on banking, since I don't know where else to put them.
    - Everyone needs access to the BG tiles so those should be in the main ROM.
    - Ship, font, and bullets will be used in several places and should be in 
      main ROM?
    - Enemy tiles are only used during game state and can be placed in their 
      own bank.
    - Bubble font for level transitions not used elsewhere, can be in its own 
      bank.
    - Some state processing can be broken out?

 [ ] Create spreadsheet/set of #defines for sprite tables
    - Break out which sprites will be used where and for what
    - This should probably go into a set of defines?
    - Title screen:
      - ??
    - Ingame (all):
      - 2 sprites for player ship
    - Ingame (normal):
      - 8 sprites for explosion SFX 
      - 4 sprites for player bullets
      - 26 sprites for enemy bullets
    - Ingame (level change):
      - 2 sprites for player ship
      - 16 sprites for upper letters
        - 8 letters of text rendered as two sprites
      - 32 sprites for lower letters
        - 16 letter of text rendered as two rows of two sprites
        - Current plan is to upate sprites midframe for top and bottom of screen

[X] Ask on Discord(s) wrt reloading sprintes midframe
    - Pan Docs say it can be glitchy. What are the details on that?
    - Can you update mid-sprite if nothing abt the sprite changes?
    - Word is you can't draw sprites while you're DMAing because it locks the
      bus. Should be possible if you turn sprites off & don't draw near the
      player's ship 
    - Thought: mask a bit or bits out of the player's position and, use
      that to determine if they're on the top or bottom half of the screen,
      then do a DMA right after the top text or right below the bottom 
      accordingly
[X] Add in player movement

- Background:
 [X] Port parallax BG stuff into game state
 [X] Create spreadsheet for parallax values
 [X] Put together precalculated tables for values
 [X] Modify parallax BG stuff to use precalculated tables for values
 [X] Update to support 2x2 tile patterns
 [X] Modify to support one tile high status menu
 [ ] Update for subpixel movement
 [ ] Experiment w/ faster movement on top and bottom bars
 [ ] Rejigger height
 [ ] Add window BG back in
 [ ] Multi-tile window BG patterns
     [ ] Figure out how to precalculate and store patterns
     [ ] Add in function to update BG pattern during HBLANK
     [ ] Add tiles to memory
         - Should these be in a specific bank to isolate them?
         - How long does switching banks take?

- Bullets
 [ ] Put together fake linked list implementation to hold bullets
 [ ] Write functions to add/remove bullets from list
 [ ] Figure out structure for bullets
 [ ] Write update loop for bullets
 [ ] Basic bullet
 [ ] Test w/ max # of bullets onscreen

- Test enemy/basic behavior
 [ ] Infrastructure for enemy
 [ ] Add enemy firing
 [ ] Expand test enemy to multiple turrets

- Player ship state machine
 [ ] Sketch out player ship state machine
 [ ] Implement

- Collision detection (bullets vs player)
 [ ] Variable or fixed size enemy bullets?
 [ ] Where to do collision detection?
 [ ] Functions for collision detection

- Collision detection (player bullets vs enemy)

- Collision detection (enemy vs player)
 - Reuse code from pbullet vs enemy?

- Enemy Damage

- Enemy Generation

- Enemy Firing
 [ ] More bullet types?

- Game Over screen

- Basic title screen

- High Score manager

- Level intro screen
 [ ] Pick font, edit if desired.
     - Custom font, still working on it. In rsrc.
 [X] Psudocode screen

 - Getting the parallaxing background is a limiter on being able to proceed here
 [ ] Implement init function
 [ ] Implement Main function
 [ ] Test midframe OAM DMA
 [ ] Move level intro into its own file?


- Splash screen
 [ ] Basic splash screen
 [X] Figure out how palette effects work
 [ ] 