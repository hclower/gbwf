To Do List

[X] Write up rough roadmap

[X] Remove temp HS handler testing code
[X] Add utitities w/ 'wait for any button' function
[X] Add automated size display to makefile

- Error handling:
 [X] Restructure main function so that each state returns a new state
 [X] Test error state
 [X] Split state code into error manager & error state (for displaying err)
 [X] Modify error codes to work like errno (global variables, not passed)

[X] Figure out subpixel movement
[X] Test subpixel speed vs normal speed
   - Looks like using structures to do subpixel operations is actually slightly
     faster than doing it with bit-shifts
     - Makes sense, bit-shifts are fast but it's still an operation the structs
       are totally skipping. Both flavors are doing 16-bit math.
   - Bit shifting does allow a larger play area- 8.8 is good enough to track
     everything onscreen but will run into problems tracking objects offscreen.
     The Simple Physics demo means that the 'pixel' portion of each word can be
     in a 1024 x 1024 area.
   - This probably does not matter for GBWF which is effectively a single-screen
     game, but it's worth rememebring for the future.

[ ] Game state skeleton

- Background:
 [ ] Port parallax BG stuff into game state
 [ ] Modify parallax BG stuff to use precalculated tables for values
 [ ] Update to support 2x2 tile patterns
 [ ] Modify to support one tile high status menu
 [ ] Update for subpixel movement
 [ ] Experiment w/ faster movement on top and bottom bars

- Bullets
 [ ] Put together fake linked list implementation to hold bullets
 [ ] Write functions to add/remove bullets from list
 [ ] Figure out structure for bullets
 [ ] Write update loop for bullets
 [ ] Basic bullet
 [ ] Test w/ max # of bullets onscreen

- Test enemy/basic behavior
 [ ] Infrastructure for enemy
 [ ] Add enemy firing
 [ ] Expand test enemy to multiple turrets

- Player ship state machine
 [ ] Sketch out player ship state machine
 [ ] Implement

- Collision detection (bullets vs player)
 [ ] Variable or fixed size enemy bullets?
 [ ] Where to do collision detection?
 [ ] Functions for collision detection

- Collision detection (player bullets vs enemy)

- Collision detection (enemy vs player)
 - Reuse code from pbullet vs enemy?

- Enemy Damage

- Enemy Generation

- Enemy Firing
 [ ] More bullet types?

- Game Over screen

- Basic title screen

- High Score manager

- Splash screen
 [ ] Basic splash screen
 [X] Figure out how palette effects work
 [ ] 